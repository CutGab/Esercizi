def bin_search(x, y):
    mid = len(x)//2

    if x[mid] == y:
        return True
    
    if x[mid] > y:

        j = mid

        bin_search(x[i:j], y)

    else:

        i = mid + 1
        bin_search[x[i:j], y]









